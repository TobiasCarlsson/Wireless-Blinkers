#ifndef CONFIG_H
#define CONFIG_H

#define WIFI_SSID "ssid" // Name of wifi, needs to be the same as the host side uses
#define WIFI_PASS "secret-password" // Wifi password, needs to be the same as the host side uses

#define rightButton D4  // Button for right turns, in my case White button
#define leftButton D3   // Button for left turns, in my case Green Button
#define brakeButton D2  // Button for braking, in my case Red button

#endif
// !! Important remove "-example" from this file name !!
